let Users;_cb9‍.x([["default",()=>_cb9‍.o]]);_cb9‍.w("../db/model/User",[["default",["Users"],function(v){Users=v}]]);

const userController = {
  index: async function (req, res) {
    const users = await Users.find({}, (err) => {
      if (err) return _cb9‍.g.console.log("erro:", err);
    });
    return res.json(users);
  },

  create: async function (req, res) {
    const {first_name, last_name, participation} = req.body;
    let nParticipation = Number(participation);
    const newUser = new Users({
      first_name,
      last_name,
      participation: nParticipation,
    });
    const retorno = await newUser.save();
    _cb9‍.g.console.log(retorno);
    return res.send(retorno);
  },
};

_cb9‍.d(userController);
